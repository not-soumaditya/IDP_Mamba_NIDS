# Core ML
torch>=2.0.0
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
seaborn>=0.12.0

# Mamba (requires NVIDIA CUDA GPU)
# Install separately: pip install causal-conv1d mamba-ssm --no-build-isolation
# Or use the pure PyTorch fallback (automatic if mamba-ssm is not installed)

# Baselines
xgboost>=2.0.0

# Backend
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
websockets>=11.0

# Dataset
kagglehub>=0.2.0
